# Oikontrol manual source

`guide.json` contains the main article from the generated manual at `modules/oikontrol/src/main/resources/Documentation/index.html` in https://github.com/oikoaudio/bitwig-oikontrol, tag `oikontrol-v2.23.0`. Heading IDs are added for website navigation. Its canonical Markdown source is `docs/user-guide.md` in that repository.

Update the canonical guide upstream, then refresh this snapshot from the matching release. Keep the website version and download link aligned with that release. Do not maintain a separate rewrite of the manual here.
